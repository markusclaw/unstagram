export default function NotificationsPage() {
  return (
    <div className="py-10">
      <h1 className="mb-3 text-2xl font-bold">Notifications</h1>
      <p className="prose-body">Nothing is demanding your attention. That's the feature.</p>
      <p className="mt-4 text-sm text-ash">
        No badges, no red dots, no "someone you barely know went live." An anti-algorithm
        app probably shouldn't nag you — so it doesn't.
      </p>
    </div>
  );
}
